import React from 'react';
import './Meal.css'

const Meal = (props) => {
    console.log(props.index);
    return (
        <div>
            <h2>Food side</h2>
        </div>
    );
};

export default Meal;